<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Aequitas – Commitments. Confidence. Clarity.</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="description" content="Aequitas is a behavioral experiment in commitments, confidence, and truth alignment." />
  <style>
    :root {
      --bg: #06080b;
      --card-bg: rgba(12, 16, 24, 0.96);
      --accent: #57c4ff;
      --accent-soft: rgba(87, 196, 255, 0.12);
      --text-main: #f5f7fb;
      --text-muted: #a5afc4;
      --border-subtle: rgba(255, 255, 255, 0.06);
      --radius-lg: 18px;
      --radius-sm: 10px;
      --shadow-soft: 0 24px 60px rgba(0, 0, 0, 0.65);
      --font-main: system-ui, -apple-system, BlinkMacSystemFont, "SF Pro Text",
        "Segoe UI", sans-serif;
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      min-height: 100vh;
      background: radial-gradient(circle at top, #101727 0, #050609 55%);
      color: var(--text-main);
      font-family: var(--font-main);
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 24px 16px;
    }

    .page {
      width: 100%;
      max-width: 880px;
      margin: 0 auto;
    }

    .card {
      background: var(--card-bg);
      border-radius: 24px;
      border: 1px solid var(--border-subtle);
      box-shadow: var(--shadow-soft);
      padding: 32px 24px 28px;
      display: grid;
      grid-template-columns: 1.4fr 1fr;
      gap: 24px;
    }

    @media (max-width: 768px) {
      .card {
        grid-template-columns: 1fr;
        padding: 24px 18px 22px;
      }
    }

    .badge {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      padding: 6px 12px;
      border-radius: 999px;
      background: var(--accent-soft);
      color: var(--accent);
      font-size: 12px;
      letter-spacing: 0.08em;
      text-transform: uppercase;
      border: 1px solid rgba(87, 196, 255, 0.35);
      margin-bottom: 18px;
    }

    .dot {
      width: 6px;
      height: 6px;
      border-radius: 999px;
      background: var(--accent);
    }

    h1 {
      font-weight: 650;
      font-size: clamp(28px, 4vw, 34px);
      line-height: 1.08;
      margin-bottom: 12px;
    }

    .subtitle {
      font-size: 14px;
      color: var(--text-muted);
      line-height: 1.55;
      margin-bottom: 20px;
    }

    .tagline {
      display: flex;
      flex-wrap: wrap;
      gap: 8px 16px;
      margin-bottom: 22px;
      font-size: 13px;
      color: var(--text-muted);
    }

    .tagline span {
      position: relative;
      padding-left: 16px;
    }

    .tagline span::before {
      content: "•";
      position: absolute;
      left: 0;
      color: var(--accent);
    }

    .list {
      list-style: none;
      display: grid;
      gap: 10px;
      margin-bottom: 26px;
    }

    .list li {
      display: flex;
      gap: 10px;
      font-size: 13px;
      color: var(--text-muted);
      line-height: 1.5;
    }

    .list li::before {
      content: "";
      width: 8px;
      height: 8px;
      margin-top: 6px;
      border-radius: 3px;
      background: linear-gradient(135deg, var(--accent), #95ffbf);
      flex-shrink: 0;
    }

    .cta-row {
      display: flex;
      flex-wrap: wrap;
      gap: 10px;
      align-items: center;
    }

    .cta-main {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 8px;
      padding: 10px 18px;
      border-radius: 999px;
      border: 1px solid rgba(87, 196, 255, 0.5);
      background: linear-gradient(135deg, #1a273c, #0c1827);
      color: var(--text-main);
      font-size: 13px;
      cursor: pointer;
      text-decoration: none;
      transition: all 0.16s ease-out;
    }

    .cta-main:hover {
      border-color: var(--accent);
      transform: translateY(-1px);
      box-shadow: 0 14px 30px rgba(0, 0, 0, 0.6);
    }

    .cta-note {
      font-size: 12px;
      color: var(--text-muted);
    }

    /* Right side */
    .side {
      border-radius: var(--radius-lg);
      border: 1px solid var(--border-subtle);
      background: radial-gradient(circle at top left, #172234 0, #070a11 52%);
      padding: 18px 18px 16px;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      gap: 14px;
    }

    .side-title {
      font-size: 12px;
      text-transform: uppercase;
      letter-spacing: 0.12em;
      color: var(--text-muted);
      margin-bottom: 4px;
    }

    .side-main {
      font-size: 13px;
      color: var(--text-main);
      line-height: 1.6;
      margin-bottom: 6px;
    }

    .side-meta {
      font-size: 11px;
      color: var(--text-muted);
      line-height: 1.4;
    }

    .email-form {
      margin-top: 6px;
      display: flex;
      flex-direction: column;
      gap: 8px;
    }

    .email-row {
      display: flex;
      gap: 6px;
    }

    @media (max-width: 480px) {
      .email-row {
        flex-direction: column;
      }
    }

    input[type="email"] {
      flex: 1;
      padding: 9px 11px;
      border-radius: 999px;
      border: 1px solid rgba(255, 255, 255, 0.17);
      background: rgba(5, 7, 11, 0.9);
      color: var(--text-main);
      font-size: 13px;
      outline: none;
      transition: border-color 0.12s ease-out, background 0.12s ease-out;
    }

    input[type="email"]::placeholder {
      color: rgba(165, 175, 196, 0.65);
    }

    input[type="email"]:focus {
      border-color: var(--accent);
      background: rgba(10, 14, 21, 0.96);
    }

    .btn-submit {
      padding: 9px 14px;
      border-radius: 999px;
      border: none;
      background: var(--accent);
      color: #020309;
      font-size: 12px;
      font-weight: 600;
      letter-spacing: 0.06em;
      text-transform: uppercase;
      cursor: pointer;
      white-space: nowrap;
      transition: background 0.14s ease-out, transform 0.14s ease-out;
    }

    .btn-submit:hover {
      background: #7dd3ff;
      transform: translateY(-1px);
    }

    .fine-print {
      font-size: 10px;
      color: var(--text-muted);
      opacity: 0.9;
    }

    footer {
      margin-top: 16px;
      text-align: center;
      font-size: 11px;
      color: var(--text-muted);
      opacity: 0.85;
    }
  </style>
</head>
<body>
  <main class="page">
    <section class="card">
      <!-- Left side: main copy -->
      <div>
        <div class="badge">
          <span class="dot"></span>
          <span>Early Access · Beta</span>
        </div>

        <h1>Aequitas: Track commitments. Calibrate confidence.</h1>

        <p class="subtitle">
          Aequitas is a lightweight experiment in sincerity&mdash;a place to log
          what you intend to do, state your confidence, and see how well it
          matches reality over time.
        </p>

        <div class="tagline">
          <span>Understand yourself</span>
          <span>Test truth gently</span>
          <span>Build real discipline</span>
        </div>

        <ul class="list">
          <li>Make concrete commitments instead of vague goals.</li>
          <li>Attach a confidence percentage and learn your calibration.</li>
          <li>Review outcomes in a simple dashboard as the system evolves.</li>
        </ul>

        <div class="cta-row">
          <a href="#notify" class="cta-main">
            Request beta invite
          </a>
          <span class="cta-note">v0 is rolling out to a small initial cohort.</span>
        </div>
      </div>

      <!-- Right side: email capture / info -->
      <aside class="side" id="notify">
        <div>
          <div class="side-title">Join the first cohort</div>
          <div class="side-main">
            Leave an email if you want to be notified when the app is ready for
            you to try. No spam, no tracking, just an invite link.
          </div>
        </div>

        <form
          class="email-form"
          method="POST"
          action="#"
        >
          <!-- TODO: Replace action="#" with your form handler (Formspree, Airtable, custom backend, etc.) -->
          <div class="email-row">
            <input
              type="email"
              name="email"
              placeholder="you@university.edu"
              required
            />
            <button type="submit" class="btn-submit">Notify me</button>
          </div>
          <p class="fine-print">
            By submitting, you agree to let us contact you once the prototype is
            live. You can opt out at any time.
          </p>
        </form>

        <div class="side-meta">
          Built for people who care about alignment between
          intention and action, not streaks and dopamine loops.
        </div>
      </aside>
    </section>

    <footer>
      Aequitas · Experimental sincerity platform · 2025
    </footer>
  </main>
</body>
</html>
